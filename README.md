# Obscurely Named Comments Project

## Running

Be sure to copy `server/.env.example` to `server/.env` and fill in the blanks.

Run `yarn start` in separate terminals in `client` and `server`. Navigate to `http://localhost:1234`.
